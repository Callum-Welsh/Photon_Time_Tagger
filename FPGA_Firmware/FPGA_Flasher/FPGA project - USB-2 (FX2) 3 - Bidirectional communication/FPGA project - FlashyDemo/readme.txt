To run FlashyDemo:
1. Configure the FPGA with the FlashyDemo bitfile matching your board.
2. Now you can either
 * Open the FlashyDemo window if it didn't already open (in FPGAconf, press CTRL-F)
or
 * Run the C/C++ code (or already compiled EXE) located in this directory.

Have fun.
