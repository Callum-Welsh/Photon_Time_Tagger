The CyUSB.h and CyUSB.lib files are required to compile the USB-2 FX2 C++ projects (when using the CyUSB driver).

Notes:
1. The SetupAPI.lib from the Windows driver SDK may also be necessary.
2. Disable the safe exception handlers if you get an error LNK2026 (linker->advanced property).
